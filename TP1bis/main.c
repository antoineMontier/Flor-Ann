#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void ecrire_regles(char* filename, int nb_blocs);






int main(){


    return 0;
}

void ecrire_regles(char* filename, int nb_blocs){
    if(nb_blocs < 1) return;
    FILE * f = fopen(filename,"w");

    








    fclose(f);
}